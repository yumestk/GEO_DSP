<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geo.dsp.module.data.mapper.DataHistoryMapper">
    <resultMap id="DataHistoryResultMap" type="com.geo.dsp.module.data.entity.DataHistory">
        <id column="id" property="id"/>
        <result column="uuid" property="uuid"/>
        <result column="data_id" property="dataId"/>
        <result column="old_data_content" property="oldDataContent"/>
        <result column="operate_type" property="operateType"/>
        <result column="operate_by" property="operateBy"/>
        <result column="operate_time" property="operateTime"/>
        <result column="is_del" property="isDel"/>
    </resultMap>

    <select id="selectByUuid" resultMap="DataHistoryResultMap">
        SELECT * FROM data_history WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </select>

    <select id="listByDataUuid" resultMap="DataHistoryResultMap">
        SELECT dh.* FROM data_history dh
                             LEFT JOIN data_sample ds ON dh.data_id = ds.id
        WHERE ds.uuid = CAST(#{dataUuid} AS uuid) AND dh.is_del = false
        ORDER BY dh.operate_time DESC
    </select>

    <insert id="insert" parameterType="com.geo.dsp.module.data.entity.DataHistory">
        INSERT INTO data_history (
            uuid, data_id, old_data_content, operate_type, operate_by, operate_time, is_del
        ) VALUES (
                     CAST(#{uuid} AS uuid), #{dataId}, #{oldDataContent}, #{operateType}, #{operateBy}, #{operateTime}, #{isDel}
                 )
    </insert>

    <update id="deleteByUuid">
        UPDATE data_history SET is_del = true, operate_by = #{operateBy}, operate_time = NOW()
        WHERE uuid = CAST(#{uuid} AS uuid)
    </update>
</mapper>
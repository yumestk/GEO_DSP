<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geo.dsp.module.data.mapper.ProjectMapper">
    <resultMap id="ProjectResultMap" type="com.geo.dsp.module.data.entity.Project">
        <id column="id" property="id"/>
        <result column="uuid" property="uuid"/>
        <result column="company_id" property="companyId"/>
        <result column="project_name" property="projectName"/>
        <result column="project_code" property="projectCode"/>
        <result column="project_address" property="projectAddress"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="manager_id" property="managerId"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="note" property="note"/>
        <result column="is_del" property="isDel"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO project (
            uuid, company_id, project_name, project_code, project_address,
            start_time, end_time, manager_id, status, create_by, create_time,
            update_by, update_time, note, is_del
        ) VALUES (
                     CAST(#{uuid} AS uuid), #{companyId}, #{projectName}, #{projectCode}, #{projectAddress},
                     #{startTime}, #{endTime}, #{managerId}, #{status}, #{createBy}, #{createTime},
                     #{updateBy}, #{updateTime}, #{note}, #{isDel}
                 )
    </insert>

    <select id="selectByUuid" resultMap="ProjectResultMap">
        SELECT * FROM project WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </select>

    <select id="listByCompanyUuid" resultMap="ProjectResultMap">
        SELECT p.* FROM project p
        LEFT JOIN companies c ON p.company_id = c.id
        WHERE c.uuid = CAST(#{companyUuid} AS uuid) AND p.is_del = false
        ORDER BY p.create_time DESC
    </select>

    <select id="listByStatus" resultMap="ProjectResultMap">
        SELECT * FROM project WHERE status = #{status} AND is_del = false
        ORDER BY create_time DESC
    </select>

    <update id="updateByUuid">
        UPDATE project
        <set>
            <if test="projectName != null">project_name = #{projectName},</if>
            <if test="projectAddress != null">project_address = #{projectAddress},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="note != null">note = #{note},</if>
        </set>
        WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </update>

    <update id="updateStatusByUuid">
        UPDATE project SET status = #{status}, update_by = #{updateBy}, update_time = NOW()
        WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </update>

    <update id="deleteByUuid">
        UPDATE project SET is_del = true, update_by = #{updateBy}, update_time = NOW()
        WHERE uuid = CAST(#{uuid} AS uuid)
    </update>

    <select id="selectPage" resultMap="ProjectResultMap">
        SELECT * FROM project
        WHERE is_del = false
        <if test="keyword != null and keyword != ''">
            AND (project_name LIKE CONCAT('%', #{keyword}, '%')
            OR project_code LIKE CONCAT('%', #{keyword}, '%')
            OR project_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        <if test="startTime != null">
            AND start_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND end_time &lt;= #{endTime}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM project
        WHERE is_del = false
        <if test="keyword != null and keyword != ''">
            AND (project_name LIKE CONCAT('%', #{keyword}, '%')
            OR project_code LIKE CONCAT('%', #{keyword}, '%')
            OR project_address LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="companyId != null">
            AND company_id = #{companyId}
        </if>
        <if test="startTime != null">
            AND start_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND end_time &lt;= #{endTime}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geo.dsp.module.data.mapper.DataLineMapper">
    <sql id="createIndex">CREATE INDEX idx_data_line_id ON data_line (id);</sql>

    <resultMap id="DataLineResultMap" type="com.geo.dsp.module.data.entity.DataLine">
        <id column="id" property="id"/>
        <result column="uuid" property="uuid"/>
        <result column="use" property="use"/>
        <result column="name" property="name"/>
        <result column="engineering_id" property="engineeringId"/>
        <result column="type" property="type"/>
        <result column="list_data_point" property="listDataPoint"/>
        <result column="plan_start_time" property="planStartTime"/>
        <result column="plan_end_time" property="planEndTime"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="note" property="note"/>
        <result column="is_del" property="isDel"/>
    </resultMap>

    <select id="selectById" resultMap="DataLineResultMap">
        SELECT * FROM data_line WHERE id = #{id} AND is_del = false
    </select>

    <select id="listByEngineeringId" resultMap="DataLineResultMap">
        SELECT * FROM data_line WHERE engineering_id = #{engineeringId} AND is_del = false ORDER BY name ASC
    </select>

    <select id="selectByLineName" resultMap="DataLineResultMap">
        SELECT * FROM data_line WHERE name = #{name} AND is_del = false
    </select>

    <insert id="insert">
        INSERT INTO data_line (
            uuid, use, name, engineering_id, type, list_data_point,
            plan_start_time, plan_end_time, status, create_by, create_time, update_by, update_time, note, is_del
        ) VALUES (
                     CAST(#{uuid} AS uuid), #{use}, #{name}, #{engineeringId}, #{type}, #{listDataPoint},
                     #{planStartTime}, #{planEndTime}, #{status}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{note}, #{isDel}
                 )
    </insert>

    <update id="update">
        UPDATE data_line
        <set>
            <if test="use != null">use = #{use},</if>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="listDataPoint != null">list_data_point = #{listDataPoint},</if>
            <if test="planStartTime != null">plan_start_time = #{planStartTime},</if>
            <if test="planEndTime != null">plan_end_time = #{planEndTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="note != null">note = #{note},</if>
        </set>
        WHERE id = #{id} AND is_del = false
    </update>

    <update id="updateStatus">
        UPDATE data_line SET status = #{status}, update_time = NOW() WHERE id = #{id} AND is_del = false
    </update>

    <update id="deleteById">
        UPDATE data_line SET is_del = true, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="batchDelete">
        UPDATE data_line SET is_del = true, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="selectByUuid" resultMap="DataLineResultMap">
        SELECT * FROM data_line WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </select>

    <update id="updateStatusByUuid">
        UPDATE data_line SET status = #{status}, update_time = NOW() WHERE uuid = CAST(#{uuid} AS uuid) AND is_del = false
    </update>

    <update id="deleteByUuid">
        UPDATE data_line SET is_del = true, update_time = NOW() WHERE uuid = CAST(#{uuid} AS uuid)
    </update>

    <select id="listByEngineeringUuid" resultMap="DataLineResultMap">
        SELECT dl.* FROM data_line dl
                             LEFT JOIN data_project dp ON dl.engineering_id = dp.id
        WHERE dp.uuid = CAST(#{engineeringUuid} AS uuid) AND dl.is_del = false
        ORDER BY dl.name ASC
    </select>
</mapper>